

# Plan: Remove "View settings preview" Button from Auth Page

## Change

Remove the button element (lines 1032-1038) in `src/pages/Auth.tsx` that shows "View settings preview (no login)".

### File: `src/pages/Auth.tsx`
- Delete the `<button>` block that navigates to `/settings-preview` (lines 1032-1038)
- No other files or logic affected

