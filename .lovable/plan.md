

# Fix TurnByTurnNav Button Text

## Change

**File:** `src/components/navigation/TurnByTurnNav.tsx`, line 633

Replace `Open in Google Maps` with `Open in Maps` to match the provider-neutral branding applied across all other map components.

This is a single-line text change. No logic, lifecycle, or styling modifications.

