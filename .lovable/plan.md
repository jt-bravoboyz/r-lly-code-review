# Plan: Permanent Mobile Video Thumbnail Fix\n\nThe issue is now isolated: the video rows still have `thumbnail_url = null`, and the browser-side backfill is failing to upload the generated thumbnails because the storage insert is blocked by row-level security. The UI is also still showing raw `<video>` previews in the Recap Tour and hero carousel, which mobile browsers can render as blank until playback/metadata loads.\n\n## 1. Stop relying on mobile browsers to create thumbnails\nCreate a backend thumbnail generation path so thumbnails are produced with privileged backend access instead of client-side storage upload.\n\n- Add a new backend function, likely `generate-video-thumbnail`, that:\n  - accepts `{ media_id }`\n  - verifies the caller is an event member before doing work\n  - fetches the video row\n  - extracts/generates a JPEG thumbnail server-side\n  - uploads it into the